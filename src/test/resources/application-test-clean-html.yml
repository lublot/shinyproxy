spring:
  session:
    store-type: none
  data:
    redis:
      repositories:
        enabled: false
proxy:
  authentication: simple
  container-backend: docker


  users:
    - name: demo
      password: demo
    - name: demo2
      password: demo2


  specs:
    - id: 01_hello
      description: <b>Test!</b> <script>alert('Test')</script>
      custom-app-details:
        - name: example
          value: "#{T(java.util.UUID).randomUUID()}"
          description: "CustomAppDetailsDescription<a onclick=\"alert('test')\">link</a><script>alert('test')</script>"
      parameters:
        definitions:
          - id: myparameter
            description: "<b>MyParameter!</b><a href=\"javascript:alert('test')\"><script>alert('test')</script>"
        value-sets:
          - values:
              myparameter:
                - myvalue
      container-image: "openanalytics/shinyproxy-integration-test-app"
